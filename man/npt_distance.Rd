% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distance.R
\name{npt_distance}
\alias{npt_distance}
\title{Squared Nonparanormal Transport Distance Between Two Distributions}
\usage{
npt_distance(summaries, first, second, decompose = FALSE, bw_weight = 1)
}
\arguments{
\item{summaries}{A \code{nonparanormal} object returned by \code{\link[=as_nonparanormal]{as_nonparanormal()}}.}

\item{first, second}{A distribution name or a one-based integer index selecting
the two distributions in \code{summaries}.}

\item{decompose}{Logical; if \code{TRUE}, returns a named list with the separate
\code{marginal}, \code{correlation}, and \code{total} distance matrices. If \code{FALSE} (default),
returns only the \code{total} squared-distance matrix.}

\item{bw_weight}{Positive finite scalar \eqn{c} multiplying the squared
Bures--Wasserstein correlation term (default is \code{1}). Changing this weight
does not alter the marginal term.}
}
\value{
A numeric scalar containing the total squared NPT distance. If
\code{decompose = TRUE}, returns a list with scalar components \code{marginal},
\code{correlation} (the weighted term \eqn{c\,\mathrm{BW}^2}), and \code{total}.
}
\description{
Computes one squared NPT distance between two distributions selected from a
common \code{nonparanormal} representation object. Using the common representation object is important
when \code{\link[=as_nonparanormal]{as_nonparanormal()}} applied pooled median/MAD preprocessing:
both the single distance and the corresponding pairwise-matrix entry then use
exactly the same global transformation.
}
\seealso{
\code{\link[=pairwise_npt_distance]{pairwise_npt_distance()}}, \code{\link[=as_nonparanormal]{as_nonparanormal()}}
}
